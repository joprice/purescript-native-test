# purescript-native-test

## Notes

- `codegenTargets` needs to be set in .vim/coc-settings.json to avoid attempting to generate
  js code. If this is not configured, errors for missing foreign imports will result.
